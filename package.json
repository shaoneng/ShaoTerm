{
  "name": "shaoterm",
  "version": "1.2.11",
  "description": "ShaoTerm: AI coding CLI multi-session manager with smart tab naming",
  "author": "shaoneng",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test tests/*.test.js",
    "rebuild": "electron-rebuild",
    "postinstall": "node scripts/fix-node-pty-helper-perms.js",
    "dist": "node scripts/fix-node-pty-helper-perms.js && electron-builder",
    "dist:mac": "node scripts/fix-node-pty-helper-perms.js && electron-builder --mac",
    "dist:mac:dir": "node scripts/fix-node-pty-helper-perms.js && electron-builder --mac --dir",
    "dist:win": "node scripts/fix-node-pty-helper-perms.js && electron-builder --win",
    "dist:all": "node scripts/fix-node-pty-helper-perms.js && electron-builder --mac --win"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "node-pty": "^1.0.0",
    "xterm": "^5.3.0",
    "xterm-addon-fit": "^0.8.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8"
  },
  "build": {
    "appId": "com.shaoneng.shaoterm",
    "productName": "ShaoTerm",
    "mac": {
      "category": "public.app-category.developer-tools",
      "target": [
        "dmg"
      ],
      "icon": "assets/icon.icns",
      "artifactName": "${productName}-${version}-${arch}.${ext}"
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icon.ico",
      "artifactName": "${productName}-${version}-Setup.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "files": [
      "main.js",
      "main/**/*",
      "preload.js",
      "renderer/**/*",
      "lib/**/*",
      "node_modules/**/*"
    ],
    "npmRebuild": false,
    "afterPack": "scripts/after-pack.js",
    "extraMetadata": {
      "main": "main.js"
    }
  }
}
